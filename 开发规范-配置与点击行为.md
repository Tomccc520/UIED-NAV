# 前后端规范：配置读取与点击行为

## 1. 目标

解决以下高频问题：

- 后台已配置但前端不生效（读取了错误的数据层级）
- 分类区域与热门推荐区域点击行为混用
- 不同页面点击逻辑分叉，导致同配置不同行为

## 2. 点击行为统一语义

### 2.1 分类区域（`websiteClickMode`）

- `detail`：进入详情页
- `direct`：直接打开外部网站

### 2.2 热门推荐区域（`hotRecommendationClickMode`）

- `detail`：进入详情页
- `direct`：直接打开外部网站

### 2.3 兼容历史值（迁移期）

- `directExternal` -> `direct`
- `modal` -> `detail`

> 要求：兼容逻辑只能作为“读/写规范化”存在，不再作为主语义继续扩散。

## 3. 前后端接口规范

## 3.1 后端返回格式

后端统一使用：

```json
{ "code": 200, "data": {}, "message": "请求成功" }
```

### 3.2 前端读取规范

- 必须先解包响应，再使用业务数据。
- 禁止直接把 `response.data` 当最终配置对象。

推荐统一解包函数：

- 入参：`payload`
- 逻辑：若含 `code` 与 `data`，返回 `data`；否则返回 `payload`

## 4. 配置流转规范

### 4.1 保存时规范化

- 后端保存 `pageGlobalConfig` 前做规范化：
  - 分类点击模式规范化
  - 热门推荐点击模式规范化

### 4.2 读取时规范化

- 后端 `public settings` 与 `frontend-config` 输出前规范化一次
- 前端构建配置时再规范化一次（双保险）

## 5. 前端实现规范

### 5.1 卡片点击（主点击区）

- 分类卡片只读 `websiteClickMode`
- 热门推荐卡片只读 `hotRecommendationClickMode`
- 两者独立，不互相回退

### 5.2 直达箭头

- 当主点击是 `direct`：箭头进入详情页
- 当主点击是 `detail`：箭头直达外站

### 5.3 新窗口策略

- 详情页跳转：使用 `detailPageNewWindow`
- 外站跳转：使用 `directArrowNewWindow`（箭头）或统一 `window.open`（主点击直达）

## 6. 代码组织规范（第一阶段）

建议按阶段整理：

1. 提取 `click-mode` 统一工具（前端）
2. 提取后端 `setting normalize` 工具（后端）
3. 全项目替换散落分支（`directExternal` / `modal`）
4. 增加最小回归测试（配置值 -> 点击行为）

## 7. 验收清单

- 后台改成“直达网站”，分类卡片点击直接外跳
- 后台改成“跳转详情页”，分类卡片点击进入详情页
- 热门推荐配置切换不影响分类区域
- 历史数据库值 `directExternal/modal` 不会导致行为错乱
- 前端构建通过（允许现存非阻断 warnings）

