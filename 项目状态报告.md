# UIED 导航系统 - 项目状态报告

**生成时间**: 2026-02-12  
**项目路径**: `/Users/tangxiaoda/Desktop/网站备份/HAO UIED`

---

## 📊 项目概况

### 技术架构
- **前端**: React 19 + TypeScript + Zustand (端口: 3003)
- **后端**: Egg.js (likeadmin) (端口: 8002)
- **管理后台**: Vue 3 + Element Plus (端口: 5174)
- **数据库**: MySQL 8.0 (端口: 3308) + Redis (端口: 6380)

### 项目结构
```
HAO UIED/
├── frontend/          # React 前端 (用户界面)
├── server/
│   ├── server/        # Egg.js 后端 API
│   └── admin/         # Vue 3 管理后台
├── docker/            # Docker 配置
├── data/              # 数据库备份
└── docs/              # 项目文档
```

---

## ✅ 检查结果

### 1. 依赖安装状态
- ✅ **前端依赖**: 已安装 (884 个包)
- ✅ **后端依赖**: 已安装 (986 个包)
- ✅ **管理后台依赖**: 已安装 (469 个包)

### 2. 端口占用情况
- ✅ **8002** (后端): 未占用
- ✅ **5174** (管理后台): 未占用
- ✅ **3003** (前端): 未占用

### 3. Docker 状态
- ⚠️ **Docker 未运行**: 需要启动 Docker Desktop

### 4. 配置文件
- ✅ 后端配置: `server/server/config/config.local.js`
- ✅ 数据库配置: MySQL (3308), Redis (6380)
- ✅ API 地址: `http://localhost:8002/api`

---

## 🚀 启动步骤

### 方法一：使用启动脚本（推荐）

1. **启动 Docker Desktop**
   ```bash
   # 打开 Docker Desktop 应用
   open -a Docker
   # 等待 Docker 启动完成（约 30 秒）
   ```

2. **运行启动脚本**
   ```bash
   cd "/Users/tangxiaoda/Desktop/网站备份/HAO UIED"
   chmod +x start.sh
   ./start.sh
   ```

### 方法二：手动启动

1. **启动 Docker Desktop**
   ```bash
   open -a Docker
   ```

2. **启动 MySQL 和 Redis**
   ```bash
   cd "/Users/tangxiaoda/Desktop/网站备份/HAO UIED"
   docker-compose -f docker/docker-compose.mysql.yml up -d
   ```

3. **启动后端服务** (新终端)
   ```bash
   cd "/Users/tangxiaoda/Desktop/网站备份/HAO UIED/server/server"
   npm run dev
   ```

4. **启动管理后台** (新终端)
   ```bash
   cd "/Users/tangxiaoda/Desktop/网站备份/HAO UIED/server/admin"
   npm run dev
   ```

5. **启动前端** (新终端)
   ```bash
   cd "/Users/tangxiaoda/Desktop/网站备份/HAO UIED/frontend"
   PORT=3003 npm start
   ```

---

## 🌐 访问地址

启动成功后，可以访问以下地址：

| 服务 | 地址 | 说明 |
|------|------|------|
| 🌐 前端网站 | http://localhost:3003 | 用户访问的导航网站 |
| 🎨 管理后台 | http://localhost:5174 | 内容管理系统 |
| 📡 后端 API | http://localhost:8002/api | RESTful API 接口 |
| 🗄️ MySQL | localhost:3308 | 数据库 |
| 🔴 Redis | localhost:6380 | 缓存服务 |

**默认管理员账号**:
- 用户名: `admin`
- 密码: `123456`

---

## 📋 核心功能

### 前端功能
- ✅ 网站分类展示
- ✅ 网站搜索
- ✅ 热门推荐
- ✅ 用户提交网站
- ✅ 响应式设计
- ✅ SEO 优化

### 管理后台功能
- ✅ 网站管理（增删改查）
- ✅ 分类管理（含子分类）
- ✅ 页面管理
- ✅ 用户管理
- ✅ 站点设置
- ✅ SEO 配置
- ✅ 数据统计
- ✅ 批量导入/导出

---

## ⚠️ 注意事项

1. **首次启动**
   - 确保 Docker Desktop 已启动
   - 首次启动 MySQL 需要等待初始化（约 30 秒）
   - 如果数据库为空，需要导入初始数据

2. **数据库初始化**
   ```bash
   # 导入基础表结构
   docker exec -i uied_mysql mysql -u uied -puied123456 uied_nav < server/sql/install.sql
   
   # 导入业务表
   docker exec -i uied_mysql mysql -u uied -puied123456 uied_nav < server/sql/uied_tables.sql
   ```

3. **端口冲突**
   - 如果端口被占用，可以修改配置文件中的端口号
   - 前端: `frontend/package.json` 的 start 脚本
   - 后端: `server/server/config/config.local.js`
   - 管理后台: `server/admin/vite.config.ts`

4. **环境变量**
   - 前端 API 地址配置在 `frontend/src/services/api.ts`
   - 默认指向 `http://localhost:8002/api`

---

## 🔧 常用命令

### 停止服务
```bash
# 停止所有 Node 服务
./stop.sh

# 停止 Docker 容器
docker-compose -f docker/docker-compose.mysql.yml down
```

### 查看日志
```bash
# 后端日志
cd server/server && npm run dev

# 查看 Docker 日志
docker logs uied_mysql
docker logs uied_redis
```

### 数据库备份
```bash
# 备份数据库
docker exec uied_mysql mysqldump -u uied -puied123456 uied_nav > backup_$(date +%Y%m%d).sql
```

---

## 📚 相关文档

- [开发指南](docs/开发指南.md)
- [数据库说明](docs/数据库说明.md)
- [宝塔部署教程](docs/宝塔部署教程.md)
- [Docker部署教程](docs/Docker部署教程.md)
- [测试指南](docs/测试指南.md)

---

## 🐛 常见问题

### 1. Docker 无法启动
**问题**: `Cannot connect to the Docker daemon`  
**解决**: 打开 Docker Desktop 应用并等待启动完成

### 2. 端口被占用
**问题**: `Error: listen EADDRINUSE`  
**解决**: 
```bash
# 查找占用端口的进程
lsof -ti:8002
# 杀死进程
kill -9 <PID>
```

### 3. 数据库连接失败
**问题**: `connect ECONNREFUSED 127.0.0.1:3308`  
**解决**: 
- 确保 Docker 容器正在运行
- 检查数据库配置是否正确
- 等待 MySQL 初始化完成

### 4. 前端无法访问 API
**问题**: `Network Error` 或 `CORS Error`  
**解决**: 
- 确保后端服务已启动
- 检查 API 地址配置
- 查看浏览器控制台错误信息

---

## 📞 技术支持

- 官网: https://fsuied.com
- 作者: Tomda (https://www.tomda.top)
- GitHub: https://github.com/Tomccc520/UIED-NAV

---

**© 2026 UIED技术团队. All Rights Reserved.**

